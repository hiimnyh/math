Year 1 sem 1 calculus 

some notation

$$\int_{a}^{b} f(x) \, dx$$

Image
we let $D$ as the domain
$I_mf=\{f(x):x\in D\}$

power function
$$x^{\alpha}$$

* $\alpha = n$ which n is a positive integer
  * Polynomial
* $\alpha = \frac{1}{n}$ 
  * Root function
* $\alpha = -1$,
  * **reciprocal** function

Rational function
* the ratio of two **polynomial** function
$$f(x)=\frac{f(x)}{g(x)}$$

Algebraic function


A function is called periodic if there is a positive p such that 
$$f(x+p)=f(x)$$
and the smallest p we call the pepriod of the function.

ex.
the period of sinx is $2\pi$
$$sin(x+2\pi)=sinx$$
but what is sin(ax)?
period become $\frac{2\pi}{a}$, since
$$sin(ax)=sin(a(x+\frac{2\pi}{a}))=sin(ax+2\pi)$$

maybe take s time to check trough all the proof of trigometric identities?

horizontal test for check is the function injective or not

neighbourhood of "a" means an open interval that contain the point "a".

try take $\delta$ as other number than 1 if 1 doesn't work for proving the limit.

to check where is a function differentiable, check all the point ,ex. for $|x|$ need to check 0 and $a\ne 0$

# Chap1
* what is strictly monotonic.
* what is power function
* what is reciprocal funtion
* what is rational function
* what is algebraic function
* what is periodic function
* what is exponential function
* what is the effect of $cf(x),\frac{1}{c}f(x),f(cx),f(\frac{x}{c})$, if $c>0$

# Chap2
* write the precise definition of limit
* write the precise definition of left,right limit
* write the precise definition of $\lim _{x\to a}f(x)=\infty$ and $\lim _{x\to a}f(x)=-\infty$
* write the definition of continuous
* Show that the function $f(x) = 1 − \sqrt{1 − x^2}$ is continuous on the
interval $[−1, 1]$.
* write down the composite Limit theorem
* Show that $h(x) = |x^2 − 3x − 6|$ is continuous at each real number.
* when finding horizontal asymtote, need to find $x\to \infty \text{ and } x\to -\infty$ both side.
* show that if f is differentialble at x=a, then it is continuous at x=a.
* If $\lim _{x\to a}g(x)=B\ne 0$
 prove that there exists a positive number d such that 0 < |x - a| < d implies $|g(x)|>\frac{|B|}{2}$

* **exercise revice**
  * 2.4
    * 39,43

# Chap3
* evalute $\lim _{x\to 0}\frac{\sin x}{x}$
* evalute $\lim _{x\to 0}\frac{1-\cos x}{x}$
* prove chain rule
* find out the different among $\Delta x,dx,\Delta y,dy$
* **exercise revice**
  * 3.5
    * 75
  * 3.9
    * 35，42
  * 3.10
    * 33，35，40

# Chap4
* write down extreme value theorem
* what is singular point
* what is critical point theorem
* what is Rolle's theorem
* what is mean value theorem
*  prove that the equation $x^3 + x − 1 = 0$ has exactly one real root .
*  show that If $F'(x) = 0$ for all x in (a, b), then F(x) is a constant C on (a, b)
*  what is concave up, concave down
*  what is inflection point
*  list down 8 step of skeching a graph
*  write down the newton's method，try to derive.
*  **exercise revice**
   *  4.2
      *  19,25, 29
   *  4.3
      *  75，83
   *  4.4
      *  55
   *  4.5
      *  73
   * 4.8
     * 17

# Chap5
* write the defintiion of riemann sum, when is the left sum situation, when is right.
* If $f$ is bounded on $[a, b]$ and if it is continuous there except at finite number
of points, then $f$ is integrable on $[a, b]$.
* If $f$ is continuous on $[a, b]$, then it is integrable on $[a, b]$.
* what is camparison property and boundeness property.
* give an extimate of $$\int _0 ^1 e^{-x^2}dx$$
* (prove the firt fundamental theorem) Let $f$ be continuous on the closed interval $[a, b]$ and let $x$ be a variable point
in $(a, b)$. Then $$\frac{d}{dx}\int _a ^x f(t)dt=f(x)$$
* prove the second fundamental theorem.
* how to calculate displacement, distant travelled
* what is symmetric theorem
* **exercise revice**
  * 5.1
    * 5
  * 5.2
    * 55, 57
  * 5.3
    * 13, 17, 75
  * 5.4
    * 45, 59, 61
  * 5.5
    * 61, 67, 73, 91, 93

# Chap6
* what is a washers
* write the concept of washers method and shell method, like how we think about the formula
* write the formula of shell method
* write the definition of average value of a function.
* what is the MVT for integral
* **exercise revice**
  * 6.1
    * 3, 15
  * 6.3
    * 5, 7, 17, **19**
  * 6.5
    * 3,5,7

# Chap7
* write the formula of integration by part.
* ask lecturer can DI method be used in exam.
* formula of 和差化积，极化和差, especially how to derive  极化和差
* for numerical integration, interms of riemann sum, there are left sum, right sum, and midpoint sum, list out the preceudre to use this method to find the approximate value.
* then there are Trapezoidal rule and Simpson's rule, derive their formula. and then also write the procedure to use those method for finding the integrate value.
* write the defintion of $$\int _a ^\infty f(x)dx$$when will it converge, when will diverge.
* write the defintion of $$\int _{-\infty} ^\infty f(x)dx$$when will it converge, when will diverge.
* write the defintion when Integrands is Discontinuous at some point. Discontinuous Integrands
* write the Comparison Test for Improper Integrals
* **exercise revice**
  * 7.1
    * 9, 15, 17, 33, 41, 61, 63, 65
  * 7.2
    * 41
  * 7.3
    * 13, 29, 37
  * 7.4
    * 15, 29, **31**, 47, 51
  * 7.5
    * 31
  * 7.7
    * **7**
  * 7.8
    * 5, 13, 23, 31, 49, 51

# Chap8
* if $f'$ is continuous on $[a,b]$, write the formula of arc length between a and b.
* write the defintion of arc length function.
* write the formula of surface area when a function rotate yaxis or xaxis.
* write the formula of The center of mass x¯ of a system of n masses m1, m2, . . ., mn located at
points x1, x2, . . ., xn along the x-axis is
* derive the formula of center mass of a graph.
* write the theorem of pappus
* **exercise revice**
  * 8.1
    * 9, 19
  * 8.2
    * 9, 13, 17
  * 8.3
    * 23，27，31，45